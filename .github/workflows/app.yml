name: One Click APK Build

on: [workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22   # ← was 20, now 22

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3
        with:
          api-level: 34
          build-tools: '34.0.0'

      - name: Install npm dependencies
        run: npm install --legacy-peer-deps

      - name: Install Capacitor Android platform
        run: npm install @capacitor/android --legacy-peer-deps

      - name: Add Android platform
        run: npx cap add android || true
       - name: Install notification plugin
  run: npm install @capacitor/local-notifications --legacy-peer-deps
      - name: Sync Capacitor
        run: npx cap sync android
      - name: Install & run icon generator
       run: |
    npm install @capacitor/assets --save-dev
    npx capacitor-assets generate --android

      - name: Ensure gradlew is executable
        run: chmod +x android/gradlew

      - name: Build debug APK
        run: |
          cd android
          ./gradlew assembleDebug

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: apk
          path: android/app/build/outputs/apk/debug/app-debug.apk